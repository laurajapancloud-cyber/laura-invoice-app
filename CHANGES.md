
## UI v2（見やすさ・SVG統一・機能整理）

### アイコン・ビジュアル
- 絵文字アイコンをSVGスプライトに統一（新規16シンボル追加、カードタイトル/タブ/ボタン/ジョブアイコン/favicon）
- テキスト記号（⚠ ✓ ✕ ▼ ＋ ✦ ⌘）は可読性のため維持
- --text-muted を #a59c8a → #b3ab9b に輔度アップ（コントラスト改善）

### PC（≥1280px）
- 作成タブを2ペイン化：左＝伝票設定＋画像取込 / 右＝読み取り結果（sticky・自前スクロール）
- 解析前は従来の1カラムに自動フォールバック（:has対応ブラウザ）
- 明細テーブル：Enterで次の行の同じ列へ移動（Shift+Enterで前の行、IME確定は除外）

### モバイル（≤1023px）
- 作成タブに「1 撮影・取込 → 2 確認・修正 → 3 保存完了」のフローステップ表示を追加
- サムネイル削除ボタンを 22px → 28px に拡大（タップ領域改善）

### 機能整理
- バーコード読取テスト（β）を取込画面→設定の「開発ツール」カードに移動
- 設定に「商品マスタ（学習済み品番）」ビューアを追加（検索・誤学習データの削除。DELETE /api/products を新設、監査ログ記録）
- 取引先コンボボックスに「この名前で新規登録」を追加（掛率を入力するだけでその場で登録）
- 分析の月別グラフをクリック→該当月の履歴にジャンプ（ドリルダウン）

## UX改善 v3 (2026-08-19)
- 今日の状態カード（作成画面上部）: 下書きの続きから再開・未確定件数・今月処理数
- ジョブ完了時にバイブ＋バックグラウンド時はプッシュ通知
- 明細サムネイルタップで元写真をピンチズーム照合
- 保存前チェック強化: 過去単価との桁違い・数量過多・重複行警告
- 削除・確定に「元に戻す」トースト（6秒）、復元API POST /api/history/{id}/restore 追加
- 通信エラー等の文言をやさしい日本語に統一、SWキャッシュ laura-v9
