
## UI v2（見やすさ・SVG統一・機能整理）

### アイコン・ビジュアル
- 絵文字アイコンをSVGスプライトに統一（新規16シンボル追加、カードタイトル/タブ/ボタン/ジョブアイコン/favicon）
- テキスト記号（⚠ ✓ ✕ ▼ ＋ ✦ ⌘）は可読性のため維持
- --text-muted を #a59c8a → #b3ab9b に輔度アップ（コントラスト改善）

### PC（≥1280px）
- 作成タブを2ペイン化：左＝伝票設定＋画像取込 / 右＝読み取り結果（sticky・自前スクロール）
- 解析前は従来の1カラムに自動フォールバック（:has対応ブラウザ）
- 明細テーブル：Enterで次の行の同じ列へ移動（Shift+Enterで前の行、IME確定は除外）

### モバイル（≤1023px）
- 作成タブに「1 撮影・取込 → 2 確認・修正 → 3 保存完了」のフローステップ表示を追加
- サムネイル削除ボタンを 22px → 28px に拡大（タップ領域改善）

### 機能整理
- バーコード読取テスト（β）を取込画面→設定の「開発ツール」カードに移動
- 設定に「商品マスタ（学習済み品番）」ビューアを追加（検索・誤学習データの削除。DELETE /api/products を新設、監査ログ記録）
- 取引先コンボボックスに「この名前で新規登録」を追加（掛率を入力するだけでその場で登録）
- 分析の月別グラフをクリック→該当月の履歴にジャンプ（ドリルダウン）

## UX改善 v3 (2026-08-19)
- 今日の状態カード（作成画面上部）: 下書きの続きから再開・未確定件数・今月処理数
- ジョブ完了時にバイブ＋バックグラウンド時はプッシュ通知
- 明細サムネイルタップで元写真をピンチズーム照合
- 保存前チェック強化: 過去単価との桁違い・数量過多・重複行警告
- 削除・確定に「元に戻す」トースト（6秒）、復元API POST /api/history/{id}/restore 追加
- 通信エラー等の文言をやさしい日本語に統一、SWキャッシュ laura-v9

## 不具合修正・改善 v4 (2026-08-19)
- 月別推移グラフ: 棒が表示されない問題を修正（ラッパー幅0pxバグ）。PCでは棒を高く、積み上げ比率も正確化
- 商品マスタ: 確定（ロック）時にも学習するように追加
- 商品マスタ: 起動時に既存の確定済み伝票を一括反映（冪等）
- 商品マスタ検索: 前方一致 → 部分一致
- SWキャッシュ laura-v10

## v5 (2026-08-19)
### 1. 解析のリアルタイム順次表示
- 解析ジョブが1枚完了するごとに進捗（n/m枚・読み取り済み明細）をジョブに記録
- 画面上部のジョブ帯とジョブパネルに「n/m枚（読み取りk点）」と実進捗バーを表示
- 抽出ボタンの上に、読み取れた品番が1枚ずつ順次現れるライブリストを追加
### 2. 下書きのサーバー同期（マルチ端末・複数下書き）
- drafts テーブルと /api/drafts API（一覧・取得・保存・削除）を追加
- 自動保存時にサーバーへも同期（オフライン時は従来どおり端末内保存が保険）
- 今日カードに「取引先名・時刻・伝票種別・点数」ラベル付きの下書き一覧を表示。タップで再開、✕で削除
- 保存完了時に該当下書きを自動削除。フォームクリア後は新規下書きとして扱う
### 3. AI精度向上ループ（学習マスタ活用）
- 学習済み品番（実績上位200件）をAI解析プロンプトへ注入し、表記ゆれをAI側で補正
- 抽出後にも O/0・I/1・B/8 等の紛らわしい文字を正規化してマスタと照合し、自動補正＋警告表示
- 価格不一致警告が自動補正警告を上書きしないようマージ方式に修正
### その他
- Service Worker キャッシュを laura-v11 に更新

## UX刷新 v6 (2026-08-20)

### 保存フローの一本化（保存＝確定）
- 「最終データを確定して保存」→「✦ 保存して確定」の1アクションに統合。保存後に自動で確定（ロック）し、6秒間の「元に戻す」トーストで取り消し可能
- 成功画面の「この内容で確定して保存（固定化）」ボタンを廃止（確定は保存時に完了）。誤確定は履歴の「解除」からいつでも復帰可
- 確定に失敗した場合は保存済みのまま「履歴の『確定』からやり直せます」を案内
- 保存後にプレビューモーダルを自動で開く挙動を廃止（成功画面のタブで同じ内容を確認できるため）

### 元写真ズームの高解像度化
- 明細サムネイルのタップ時、セッション中は元ファイル（フル解像度）をピンチズーム表示（320pxサムネ拡大によるぼやけを解消）
- 元ファイルが無い場合（下書き復元後・再読込後など）は従来どおりサムネイルにフォールバック
- 読み取り結果上部の元写真プレビューも同様に高解像度化

### ダウンロードUIの統一
- 成功画面・プレビューモーダル・履歴詳細に分散していたPDF/Excel/明細のダウンロードボタン群を「ダウンロード ▾」メニューに集約
- 履歴詳細のZIPボタンもメニュー内「ZIP一括（PDF＋Excel）」に統合
- Drive関連の表記を「Driveに保存」に統一（旧: Google Driveに4ファイル保存／Drive保存／保存 Drive／☁️ Drive）

### 伝票種別の整理（5ボタン→3ボタン＋仮トグル）
- 納品／返品／明細表の3ボタン＋「仮」トグルに変更（仮納品・仮返品はトグルで表現）
- 明細表選択時は「仮」トグルを自動で無効化

### 長押し操作の廃止
- 履歴の「確定 (長押し)」「削除 (長押し)」を通常タップに変更（誤操作はUndoトーストで復元できるため）

### 表記の統一ほか
- 未確定伝票のステータスを「下書き」→「未確定」に変更（自動保存の下書きと区別）
- モバイル保存ボタン「保存」→「✦ 保存して確定」、フローステップ「保存完了」→「確定完了」
- Service Worker キャッシュを laura-v12 に更新

## UX改善 v7 (2026-08-20)

### 明細カード（スマホ）の操作性向上
- カードを開かずに数量を修正できるミニステッパー（−/＋）を折りたたみ状態のカードに追加（スキャン後の修正で最も多いのが数量のため）
- 複数明細のとき「すべて開く／すべて閉じる」ボタンで一括開閉できるように

### 履歴の状態フィルタ
- 「すべての状態／未確定／確定済」の絞り込みを追加（既存の種別・ユーザー・月フィルタや検索と組み合わせ可）

### セッション切れ案内の改善
- ログイン期限切れ時のメッセージを「入力中の内容は自動保存されています。画面を再読み込みしてログインすると、下書きから続きを再開できます」に変更（作業が消えたように見える不安を解消）

### Undoの初回ヒント
- はじめて取り消し可能な操作をしたとき、トーストに「元に戻す」の使い方ヒントを一度だけ表示（その回のみ表示時間を10秒に延長）

### その他
- Service Worker キャッシュを laura-v13 に更新

## v8 (2026-08-20) — デザイン改修

- **通知トーストを1系統に統一**: 従来は「作成画面カード内の埋め込みトースト」と「デスクトップ専用の右下スタック」の2系統が混在し、履歴タブで確定・削除した際にUndo（元に戻す）トーストが見えない問題があった。すべてのトースト（通常・Undo付き）を画面固定スタック表示に統一。デスクトップは右下、モバイルは画面下部中央（アクションバーの上・セーフエリア対応）に表示され、どのタブからでも見える。不透明背景＋影付きで視認性も向上
- **最小文字サイズを12pxへ引き上げ**: 10〜11px・10.5pxの計50箇所を12pxに統一。ダッシュボードの極小目盛りラベル（旧8〜9pxの6箇所）のみ、レイアウト維持のため10pxに留めた
- **タップ領域の拡大**: 小ボタン（.btn-sm）min-height 38px、明細削除ボタン 36×40px、数量ステッパー 40px、カード内ミニステッパー 34px、ダウンロードメニュー項目 42px、Undoボタン 36px に拡大
- **金額の桁揃え**: 全体に等幅・ライニング数字（tabular-nums / lnum）を適用。金額や数量の桁が縦に揃い、明細・履歴・ダッシュボードの数値が見比べやすくなった
- **スタイル整理（保守改修）**: 取引先編集フォーム4箇所のハードコード色（#1a1a1a等）インラインstyleを `.input-plain` クラスに集約しテーマ変数化（ライトテーマ表示も正常化）。フォームラベル4箇所を `.field-label` に集約。履歴フィルタ3箇所のインラインstyleをCSSへ移動。インラインstyle合計 250→239箇所
- Service Worker キャッシュを laura-v14 に更新

## v8.1 (2026-08-20) — バグ修正

- **文字化け修復**: ファイル内に混入していた置換文字（U+FFFD）を6箇所修復。ユーザーに見える箇所はカメラ撮影オーバーレイの「キャンセル」と保存確定トーストの「伝票を保存して確定しました」の2件（ほか4件はコード内コメント）
- **「仮」トグルのライトモード表示バグ修正**: 背景色が #171717 固定だったためライトモードで黒いボタンになっていた。テーマ変数（var(--bg-input)）に変更し、ライトモード専用のアクティブ配色（濃い青文字・淡い青背景）も追加
- Service Worker キャッシュを laura-v15 に更新
